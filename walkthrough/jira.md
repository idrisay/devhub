## Jira

DevHub uses your Atlassian account email plus an API token. Create the token at **id.atlassian.com → Security → API tokens**.

The token is stored in VS Code's secret storage and never written to settings.

The issue for your branch appears under **Current work**, with a status pill you can click to transition it.
