## Figma

Create a personal access token at **Settings → Security → Personal access tokens**.

Any Figma frame URL in the ticket description or comments is rendered in the **Designs** panel, with the colours and type used in the frame.

If the Figma desktop app is running with its MCP server enabled, DevHub registers it so agent mode can read designs directly.
