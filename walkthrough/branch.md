## Start work

**DevHub: Start work on issue…** lists your Jira queue (configurable via `devhub.jira.jql`), creates a branch named from `devhub.branchTemplate`, and offers to move the ticket to In progress.

If your branches don't contain ticket keys, use **DevHub: Pin ticket…** — pins are remembered per branch.
